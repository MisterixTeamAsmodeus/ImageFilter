## Светлая тема UI для ImageFilter-GUI

### Базовая палитра

- **Фон панелей**: `#F8F9FA`
- **Фон центральных областей / карточек**: `#FFFFFF`
- **Основной текст**: `#212529`
- **Второстепенный текст**: `#6C757D`
- **Границы полей и контролов**: `#CED4DA`
- **Разделители**: `#E0E0E0`
- **Акцентный цвет**: `#6C5CE7`
- **Успех / Ошибка**: `#28A745` / `#DC3545`

### Организация стилей

- Глобальные стили описаны в файле `style/light_theme.qss`.
- Файл подключается из ресурсов `style.qrc` и применяется в `main.cpp`
  через `QApplication::setStyleSheet`.
- Основные элементы, которые настраиваются в QSS:
  - `QLabel`, `QCheckBox`, `QRadioButton` — цвета текста.
  - `QLineEdit`, `QSpinBox`, `QDoubleSpinBox`, `QComboBox` — фон, рамка, скругление.
  - `QToolTip` — фон подсказок и рамка.
  - `QStatusBar` — фон и цвет текста статус-бара.
  - `QPushButton#addFilterButton` — акцентная кнопка добавления фильтра.
  - `QPushButton#changeTypeButton`, `#moveUpButton`, `#moveDownButton`, `#removeButton` —
    компактные кнопки управления в карточке фильтра.
  - `QSlider` (горизонтальный) — groove, заполненная часть (`sub-page`) и ползунок.

### Карточки фильтров

- Разметка карточки описана в `gui/FilterItemWidget.ui`.
- Основные визуальные настройки:
  - Фон `#FFFFFF`, рамка `1px solid #E0E0E0`, скругление `12px` (через `styleSheet` для `#FilterItemWidget`).
  - Внутренние отступы `12–12–12–12` для `mainLayout`.
  - Под заголовком добавлен `QFrame#headerSeparator` высотой 1 px (`#E0E0E0`).
  - Слева от названия фильтра размещён `QLabel filterIconLabel` для небольшой иконки.
- Логика:
  - Заголовок и иконка привязаны к типу фильтра в `FilterItemWidget.cpp`
    (функции `getFilterDisplayName` и `resolveFilterIconName`). При смене типа
    фильтра иконка и название обновляются автоматически.

### Панель цепочки фильтров

- Разметка панели — `gui/FilterChainPanel.ui`.
- Внешние отступы `12 px`, фон списка фильтров устанавливается через стиль `#filterListWidget`.
- Между карточками фильтров в `filterListLayout` установлен вертикальный `spacing` 10 px.

### Область предпросмотра и дроп-зона

- Виджет предпросмотра реализован в `gui/PreviewWidget.{h,cpp}`.
- Состояние «нет изображения»:
  - Текст: `🖼️` + «Выберите изображение» курсивом (`pointSize ~ 11`, цвет `#6C757D`).
  - Пунктирная рамка `2px dashed #CED4DA`, фон `#FFFFFF`.
  - При наведении рамка усиливается (`#A0A4AA`), фон становится `#F8F9FA`
    (`enterEvent` / `leaveEvent` и метод `updateEmptyStateAppearance`).
- При наличии изображения метка очищается и отображается масштабированное изображение
  с сохранением пропорций (`updateImageDisplay`).

### Статус-бар и прогресс

- Логика статус-бара реализована в `gui/MainWindow.{h,cpp}`:
  - В `setupStatusBar` создаётся `QProgressBar` (`processingProgressBar_`) и добавляется
    как постоянный виджет в `QStatusBar`.
  - В `onProcessingStarted` прогресс-бар показывается и работает в режиме
    неопределённого прогресса (range `0..0`), затем переключается на `0..100` при
    поступлении процентов.
  - Время обработки измеряется через `QElapsedTimer processingTimer_`.
  - Метод `showProcessingStatus` формирует сообщения вида:
    - «✅ Изображение обработано за 0.5 сек» — при успехе.
    - «⚠️ Ошибка обработки: ...» — при ошибках.

### Адаптивность

- Для `FilterItemWidget` реализовано автоматическое сворачивание параметров
  при узкой ширине:
  - Пороговое значение ширины — 260 px (`updateParametersVisibilityByWidth`).
  - При ширине меньше порога параметры скрываются и стрелка поворота меняется
    на `Qt::RightArrow`; при расширении — возвращается `Qt::DownArrow`.
- В `MainWindow::setupCentralWidget` заданы минимальные ширины для панели фильтров
  и предпросмотра, чтобы они не схлопывались при сильном сжатии окна.


